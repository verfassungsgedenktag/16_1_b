#ifndef array_h
#define array_h

#include <stdio.h>

/* Ввод массива */
void  array_in(double *array, size_t n);

/* Вывод массива */
void array_out(const double *array, size_t n);

#endif /* array_h */
